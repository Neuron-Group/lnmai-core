-- Package root marker. Proof modules are separate `Verification.*` modules and
-- are built by Lake's default submodule globs; keep this file minimal so that
-- editing a proof module does not force re-elaborating everything.
import Verification.Basic
